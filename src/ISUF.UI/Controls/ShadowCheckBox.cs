using Windows.UI.Xaml.Controls;

namespace ISUF.UI.Controls
{
    public sealed class ShadowCheckBox : CheckBox
    {
        public ShadowCheckBox()
        {
            DefaultStyleKey = typeof(ShadowCheckBox);
        }
    }
}