using Windows.UI.Xaml.Controls;

namespace ISUF.UI.Controls
{
    public sealed class MainPageButton : Button
    {
        public MainPageButton()
        {
            DefaultStyleKey = typeof(MainPageButton);
        }
    }
}